% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vb_auth.R
\name{vb_set_token}
\alias{vb_set_token}
\title{Set Bearer token(s) for authenticated API requests}
\usage{
vb_set_token(access_token = NULL, refresh_token = NULL, tokens = NULL)
}
\arguments{
\item{access_token}{(character) Access token string.}

\item{refresh_token}{(character) Refresh token string.}

\item{tokens}{(list | character) Named list or JSON string containing
\code{access_token} and/or \code{refresh_token}. Cannot be combined with
\code{access_token} or \code{refresh_token}.}
}
\description{
Stores an OAuth2 access token and/or refresh token in global options for use in subsequent authenticated API requests. Use \code{\link[=vb_unset_token]{vb_unset_token()}} to clear previously stored tokens.
}
\details{
Two input modes are supported — use one or the other, not both:
\itemize{
\item \strong{Individual strings:} pass \code{access_token}, \code{refresh_token}, or both.
\item \strong{Token dict:} pass \code{tokens} as a named list or JSON string with \code{access_token} and/or \code{refresh_token} keys.
}
}
\examples{
vb_set_token(access_token = "eyJhbGciOiJIUzI1NiJ9...")
vb_set_token(access_token = "eyJ...", refresh_token = "eyJ...")
vb_set_token(tokens = list(access_token = "eyJ...", refresh_token = "eyJ..."))
vb_set_token(tokens = '{"access_token": "eyJ...", "refresh_token": "eyJ..."}')
}
\seealso{
\code{\link[=vb_unset_token]{vb_unset_token()}}, \code{\link[=vb_refresh_tokens]{vb_refresh_tokens()}}
}
